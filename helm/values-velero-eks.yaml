# helm/values-velero-eks.yaml

configuration:
  backupStorageLocation:
    - name: s3-primary
      provider: aws
      bucket: srespace-orion-velero-prod
      config:
        region: us-east-1
  volumeSnapshotLocation:
    - name: aws-default
      provider: aws

# Node Agent replaces Restic in newer versions
deployNodeAgent: true

# IRSA (no static secret needed, AWS creds via IAM role)
credentials:
  useSecret: false

serviceAccount:
  server:
    create: true
    annotations:
      eks.amazonaws.com/role-arn: ""  # will be set dynamically in workflow

